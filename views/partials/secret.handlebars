 <div class="cookieContainer" >


    {{#if alert}}
        <p>{{alert}}</p> 
    {{/if}}
    <form class="terms centerContainer column" method="POST" action="/secret" id="agreement"> 
        <div class="userdetails column">
            <input name="username" id="username" autocomplete="off" placeholder="whisper a secret"></input>
        </div>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <button class="btn" id="loginbtn" name="yes">share</button>
    </form>
  


</div>


